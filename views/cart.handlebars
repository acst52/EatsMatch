{{!-- Cart handlebar with its own path --}}

<div id="cart" class="container">
    <div class="cart-content">
        <h4>Cart</h4>
        <div class="item">
            <span class="item-name">{{!--data from session--}}</span>
            <span class="item-price">{{!--data from session--}}</span>
        </div>
        <div class="totalPrice">{{!--total cost for order--}}</div>
        {{!-- when checkout is clicked, render the stripe.hb --}}
        <button class="checkout-button" onclick="/path to stripe">Checkout</button>
    </div>
</div>