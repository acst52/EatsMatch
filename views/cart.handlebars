{{!-- maybe this could be a partial modal that hovers over the right side of page? --}}

{{!-- re: added a model in main.db, when cart btn is clicked the model will show, checkout btn should direct to stripe.hb to checkout --}}