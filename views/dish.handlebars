<section class="container">
  <h3 class="dish-name">{{dish.dish_name}}</h3>
  <section class="flex-row justify-center">
    {{#each dishes}}
      {{> dishDetails}}
    {{/each}}
  </section>
</section>