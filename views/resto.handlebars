<section class="container">
    <h3 class="resto-name">{{resto.name}}</h3>
    <section class="flex-row justify-center">
        {{#each resto.name as |name|}}
        {{> restoDetails}}
        {{/each}}
    </section>
</section>